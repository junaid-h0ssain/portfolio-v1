---
import { getCollection } from 'astro:content';
import type { CollectionEntry } from 'astro:content';
import { Image } from 'astro:assets';
import BaseLayout from '../../components/layout/BaseLayout.astro';
import { withBase } from '../../lib/utils';

export async function getStaticPaths() {
  const projects = await getCollection('projects');
  return projects.map((project) => ({
    params: { slug: project.slug },
    props: { project },
  }));
}

interface Props {
  project: CollectionEntry<'projects'>;
}

const { project } = Astro.props;
const { Content } = await project.render();
const { 
  title, 
  description, 
  technologies, 
  repositoryUrl, 
  liveUrl, 
  thumbnail,
  featured,
  startDate,
  endDate 
} = project.data;

// Format dates
const formatDate = (date: Date) => {
  return new Date(date).toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'short'
  });
};

const formattedStartDate = formatDate(startDate);
const formattedEndDate = endDate ? formatDate(endDate) : 'Present';
const projectDuration = `${formattedStartDate} - ${formattedEndDate}`;
---

<BaseLayout title={title} description={description} image={thumbnail}>
  <!-- Hero Section -->
  <div class="project-hero">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8 max-w-6xl">
      <div class="project-hero-inner">
        <!-- Featured badge -->
        {featured && (
          <div class="mb-4">
            <span class="featured-badge">
              <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5" viewBox="0 0 24 24" fill="currentColor">
                <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
              </svg>
              Featured Project
            </span>
          </div>
        )}

        <!-- Title -->
        <h1 class="project-title">{title}</h1>

        <!-- Description -->
        <p class="project-description">{description}</p>

        <!-- Meta row -->
        <div class="project-meta">
          <div class="meta-item">
            <svg xmlns="http://www.w3.org/2000/svg" class="meta-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/>
            </svg>
            <span>{projectDuration}</span>
          </div>

          <div class="meta-item">
            <svg xmlns="http://www.w3.org/2000/svg" class="meta-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/>
            </svg>
            <span>{technologies.length} Technologies</span>
          </div>
        </div>

        <!-- Action buttons -->
        <div class="action-buttons">
          {liveUrl && (
            <a href={liveUrl} target="_blank" rel="noopener noreferrer" class="btn btn-primary">
              <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/>
              </svg>
              View Live Demo
            </a>
          )}
          <a href={repositoryUrl} target="_blank" rel="noopener noreferrer" class="btn btn-secondary">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/>
            </svg>
            View Repository
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Main Content -->
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 max-w-6xl py-10">
    <div class="project-layout">
      <!-- Main content area -->
      <article class="project-content">
        <!-- Project thumbnail -->
        <div class="project-thumbnail">
          <Image 
            src={thumbnail} 
            alt={`${title} screenshot`}
            width={1200}
            height={675}
            class="rounded-xl shadow-lg"
            loading="eager"
          />
        </div>

        <!-- Technologies used -->
        <div class="tech-section">
          <h2 class="section-heading">Technologies Used</h2>
          <div class="tech-grid">
            {technologies.map((tech) => (
              <span class="tech-badge">{tech}</span>
            ))}
          </div>
        </div>

        <!-- MDX Content -->
        <div class="prose prose-lg dark:prose-invert max-w-none
                    prose-headings:font-bold prose-headings:text-white-smoke
                    prose-h2:text-2xl prose-h2:mt-10 prose-h2:mb-4
                    prose-h3:text-xl prose-h3:mt-8 prose-h3:mb-3
                    prose-p:text-white-smoke/85 prose-p:leading-relaxed
                    prose-a:text-dusty-taupe prose-a:no-underline hover:prose-a:underline
                    prose-code:text-dusty-taupe-200 prose-code:bg-jet-black/55 prose-code:px-1.5 prose-code:py-0.5 prose-code:rounded prose-code:text-sm
                    prose-pre:relative prose-pre:bg-black prose-pre:text-white-smoke prose-pre:rounded-xl prose-pre:shadow-lg
                    prose-strong:text-white-smoke
                    prose-ul:text-white-smoke/85
                    prose-ol:text-white-smoke/85
                    prose-li:text-white-smoke/85
                    prose-blockquote:border-dusty-taupe prose-blockquote:text-white-smoke/75
                    prose-img:rounded-lg prose-img:shadow-md">
          <Content />
        </div>

        <!-- Footer -->
        <footer class="project-footer">
          <div class="project-footer-inner">
            <a href={withBase('/projects')} class="back-btn">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/>
              </svg>
              Back to Projects
            </a>

            <button id="share-btn" class="share-btn" title="Copy link to clipboard">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/>
              </svg>
              <span id="share-label">Copy link</span>
            </button>
          </div>
        </footer>
      </article>

      <!-- Sidebar with project info -->
      <aside class="project-sidebar" aria-label="Project information">
        <div class="sidebar-inner">
          <h3 class="sidebar-heading">Project Details</h3>
          
          <div class="sidebar-section">
            <h4 class="sidebar-label">Duration</h4>
            <p class="sidebar-value">{projectDuration}</p>
          </div>

          <div class="sidebar-section">
            <h4 class="sidebar-label">Status</h4>
            <p class="sidebar-value">{endDate ? 'Completed' : 'In Progress'}</p>
          </div>

          {featured && (
            <div class="sidebar-section">
              <h4 class="sidebar-label">Highlight</h4>
              <p class="sidebar-value">Featured Project</p>
            </div>
          )}

          <div class="sidebar-divider"></div>

          <div class="sidebar-links">
            {liveUrl && (
              <a href={liveUrl} target="_blank" rel="noopener noreferrer" class="sidebar-link">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/>
                </svg>
                Live Demo
              </a>
            )}
            <a href={repositoryUrl} target="_blank" rel="noopener noreferrer" class="sidebar-link">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/>
              </svg>
              Repository
            </a>
          </div>
        </div>
      </aside>
    </div>
  </div>
</BaseLayout>

<style>
  /* ── Hero ── */
  .project-hero {
    background: linear-gradient(135deg,
      var(--jet-black) 0%,
      var(--stone-brown) 45%,
      var(--black) 100%);
    padding: 3.5rem 0 3rem;
    border-bottom: 1px solid var(--color-dusty-taupe-700);
    animation: fadeUp 0.5s ease both;
  }
  :global(.dark) .project-hero {
    background: linear-gradient(135deg,
      var(--jet-black) 0%,
      var(--stone-brown) 40%,
      var(--black) 100%);
    border-bottom-color: var(--color-dusty-taupe-700);
  }
  .project-hero-inner {
    max-width: 780px;
  }

  .featured-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    padding: 0.375rem 0.875rem;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    background: linear-gradient(135deg, var(--color-dusty-taupe-400), var(--color-dusty-taupe-600));
    color: var(--black);
    border-radius: 9999px;
    box-shadow: 0 2px 8px var(--color-dusty-taupe-500);
  }
  :global(.dark) .featured-badge {
    background: linear-gradient(135deg, var(--color-dusty-taupe-400), var(--color-dusty-taupe-600));
    color: var(--black);
  }

  .project-title {
    font-size: clamp(1.75rem, 4vw, 2.75rem);
    font-weight: 800;
    line-height: 1.15;
    letter-spacing: -0.02em;
    color: var(--white-smoke);
    margin-bottom: 1rem;
  }
  :global(.dark) .project-title { color: var(--white-smoke); }

  .project-description {
    font-size: 1.125rem;
    line-height: 1.7;
    color: var(--color-white-smoke-200);
    margin-bottom: 1.5rem;
  }
  :global(.dark) .project-description { color: var(--color-white-smoke-200); }

  .project-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
    margin-bottom: 2rem;
  }
  .meta-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--color-white-smoke-300);
  }
  :global(.dark) .meta-item { color: var(--color-white-smoke-400); }
  .meta-icon { width: 1.125rem; height: 1.125rem; flex-shrink: 0; }

  .action-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.625rem 1.25rem;
    font-size: 0.9375rem;
    font-weight: 600;
    border-radius: 0.5rem;
    text-decoration: none;
    transition: all 0.2s;
  }
  .btn-primary {
    background: var(--dusty-taupe);
    color: var(--black);
    border: 1px solid var(--color-dusty-taupe-400);
  }
  .btn-primary:hover {
    background: var(--color-dusty-taupe-400);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px var(--color-dusty-taupe-600);
  }
  .btn-secondary {
    background: var(--jet-black);
    color: var(--white-smoke);
    border: 1px solid var(--color-stone-brown-700);
  }
  .btn-secondary:hover {
    background: var(--color-stone-brown-800);
    border-color: var(--color-dusty-taupe-600);
  }
  :global(.dark) .btn-secondary {
    background: var(--jet-black);
    color: var(--white-smoke);
    border-color: var(--color-stone-brown-700);
  }
  :global(.dark) .btn-secondary:hover {
    background: var(--color-stone-brown-800);
  }

  /* ── Layout ── */
  .project-layout {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem;
    align-items: start;
  }
  @media (min-width: 1024px) {
    .project-layout {
      grid-template-columns: minmax(0, 1fr) 280px;
    }
  }

  .project-content {
    min-width: 0;
  }

  /* ── Thumbnail ── */
  .project-thumbnail {
    margin-bottom: 2.5rem;
  }

  /* ── Technologies ── */
  .tech-section {
    margin-bottom: 2.5rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid var(--color-stone-brown-800);
  }
  :global(.dark) .tech-section { border-bottom-color: var(--color-stone-brown-800); }

  .section-heading {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--white-smoke);
    margin-bottom: 1rem;
  }
  :global(.dark) .section-heading { color: var(--white-smoke); }

  .tech-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 0.625rem;
  }
  .tech-badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    font-size: 0.875rem;
    font-weight: 600;
    background: var(--color-dusty-taupe-800);
    color: var(--white-smoke);
    border: 1px solid var(--color-dusty-taupe-600);
    border-radius: 0.5rem;
    transition: all 0.15s;
  }
  .tech-badge:hover {
    background: var(--color-dusty-taupe-700);
    transform: translateY(-1px);
  }
  :global(.dark) .tech-badge {
    background: var(--color-dusty-taupe-800);
    color: var(--white-smoke);
    border-color: var(--color-dusty-taupe-600);
  }
  :global(.dark) .tech-badge:hover {
    background: var(--color-dusty-taupe-700);
  }

  /* ── Sidebar ── */
  .project-sidebar {
    display: none;
  }
  @media (min-width: 1024px) {
    .project-sidebar {
      display: block;
    }
    .sidebar-inner {
      position: sticky;
      top: 5.5rem;
      padding: 1.5rem;
      background: var(--jet-black);
      border: 1px solid var(--color-stone-brown-800);
      border-radius: 0.75rem;
      box-shadow: 0 1px 3px rgb(0 0 0 / 0.06);
    }
    :global(.dark) .sidebar-inner {
      background: var(--jet-black);
      border-color: var(--color-stone-brown-800);
    }
  }

  .sidebar-heading {
    font-size: 1rem;
    font-weight: 700;
    color: var(--white-smoke);
    margin-bottom: 1.25rem;
    padding-bottom: 0.75rem;
    border-bottom: 1px solid var(--color-stone-brown-800);
  }
  :global(.dark) .sidebar-heading {
    color: var(--white-smoke);
    border-bottom-color: var(--color-stone-brown-800);
  }

  .sidebar-section {
    margin-bottom: 1.25rem;
  }
  .sidebar-label {
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--color-white-smoke-400);
    margin-bottom: 0.375rem;
  }
  :global(.dark) .sidebar-label { color: var(--color-white-smoke-400); }

  .sidebar-value {
    font-size: 0.9375rem;
    font-weight: 500;
    color: var(--color-white-smoke-100);
  }
  :global(.dark) .sidebar-value { color: var(--color-white-smoke-100); }

  .sidebar-divider {
    height: 1px;
    background: var(--color-stone-brown-800);
    margin: 1.5rem 0;
  }
  :global(.dark) .sidebar-divider { background: var(--color-stone-brown-800); }

  .sidebar-links {
    display: flex;
    flex-direction: column;
    gap: 0.625rem;
  }
  .sidebar-link {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.625rem 0.875rem;
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--white-smoke);
    background: var(--color-dusty-taupe-900);
    border: 1px solid var(--color-dusty-taupe-700);
    border-radius: 0.5rem;
    text-decoration: none;
    transition: all 0.15s;
  }
  .sidebar-link:hover {
    background: var(--color-dusty-taupe-800);
    transform: translateX(2px);
  }
  :global(.dark) .sidebar-link {
    color: var(--white-smoke);
    background: var(--color-dusty-taupe-900);
    border-color: var(--color-dusty-taupe-700);
  }
  :global(.dark) .sidebar-link:hover {
    background: var(--color-dusty-taupe-800);
  }

  /* ── Footer ── */
  .project-footer {
    margin-top: 3rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--color-stone-brown-800);
  }
  :global(.dark) .project-footer { border-top-color: var(--color-stone-brown-800); }

  .project-footer-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 0.75rem;
  }
  .back-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--white-smoke);
    background: var(--color-dusty-taupe-900);
    border: 1px solid var(--color-dusty-taupe-700);
    border-radius: 0.5rem;
    text-decoration: none;
    transition: background 0.15s, border-color 0.15s;
  }
  .back-btn:hover {
    background: var(--color-dusty-taupe-800);
    border-color: var(--color-dusty-taupe-600);
  }
  :global(.dark) .back-btn {
    color: var(--white-smoke);
    background: var(--color-dusty-taupe-900);
    border-color: var(--color-dusty-taupe-700);
  }
  :global(.dark) .back-btn:hover {
    background: var(--color-dusty-taupe-800);
  }

  .share-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--white-smoke);
    background: var(--jet-black);
    border: 1px solid var(--color-stone-brown-800);
    border-radius: 0.5rem;
    cursor: pointer;
    transition: background 0.15s, border-color 0.15s, color 0.15s;
  }
  .share-btn:hover {
    background: var(--color-stone-brown-900);
    border-color: var(--color-dusty-taupe-600);
  }
  :global(.dark) .share-btn {
    color: var(--white-smoke);
    background: var(--jet-black);
    border-color: var(--color-stone-brown-800);
  }
  :global(.dark) .share-btn:hover {
    background: var(--color-stone-brown-900);
  }

  /* ── Animations ── */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(12px); }
    to   { opacity: 1; transform: translateY(0); }
  }
</style>

<script>
  // ── Code block: language badge + copy button ──
  document.querySelectorAll('pre').forEach(pre => {
    const code = pre.querySelector('code');
    const lang = pre.getAttribute('data-language') ?? (code?.className ?? '').match(/language-(\w+)/)?.[1];

    const wrapper = document.createElement('div');
    wrapper.style.cssText = 'position:relative';
    pre.parentNode?.insertBefore(wrapper, pre);
    wrapper.appendChild(pre);

    if (lang) {
      const badge = document.createElement('span');
      badge.textContent = lang;
      badge.style.cssText = `
        position:absolute; top:0.6rem; left:0.9rem;
        font-size:0.65rem; font-weight:700; letter-spacing:0.08em;
        text-transform:uppercase; color:var(--color-white-smoke-400);
        pointer-events:none; user-select:none;
      `;
      wrapper.appendChild(badge);
    }

    const copyBtn = document.createElement('button');
    copyBtn.textContent = 'Copy';
    copyBtn.style.cssText = `
      position:absolute; top:0.55rem; right:0.7rem;
      padding:0.2rem 0.6rem; font-size:0.7rem; font-weight:600;
      background:var(--jet-black); color:var(--white-smoke);
      border:1px solid var(--color-stone-brown-800); border-radius:0.375rem;
      cursor:pointer; transition:background 0.15s;
    `;
    copyBtn.onmouseenter = () => copyBtn.style.background = 'var(--color-stone-brown-900)';
    copyBtn.onmouseleave = () => copyBtn.style.background = 'var(--jet-black)';
    copyBtn.onclick = async () => {
      await navigator.clipboard.writeText(code?.innerText ?? '');
      copyBtn.textContent = 'Copied!';
      copyBtn.style.color = 'var(--color-dusty-taupe-400)';
      setTimeout(() => {
        copyBtn.textContent = 'Copy';
        copyBtn.style.color = 'var(--white-smoke)';
      }, 1800);
    };
    wrapper.appendChild(copyBtn);
  });

  // ── Share / copy-link button ──
  document.getElementById('share-btn')?.addEventListener('click', async () => {
    await navigator.clipboard.writeText(window.location.href);
    const label = document.getElementById('share-label');
    if (label) {
      label.textContent = 'Copied!';
      setTimeout(() => { label.textContent = 'Copy link'; }, 1800);
    }
  });
</script>
